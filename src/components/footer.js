import React from "react"

export default () => (
  <footer style={{ textAlign: `right` }}>
    <p style={{ fontSize: `0.5em`, margin: `0` }}>Created by FakeName</p>
    <p style={{ fontSize: `0.5em`, margin: `0` }}>
      Site built with Gatsby and Typography.js
    </p>
  </footer>
)
